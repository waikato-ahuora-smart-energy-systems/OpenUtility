[tool.bumpversion]
current_version = "0.1.2"
parse = "(?P<major>\\d+)\\.(?P<minor>\\d+)\\.(?P<patch>\\d+)"
serialize = ["{major}.{minor}.{patch}"]
regex = false
ignore_missing_version = false
tag = false
sign_tags = false
tag_name = "v{new_version}"
tag_message = "Bump OpenUtility project version: {current_version} -> {new_version}"
allow_dirty = false
commit = true
message = "Bump OpenUtility project version: {current_version} -> {new_version}"
commit_args = ""

[[tool.bumpversion.files]]
filename = "./pyproject.toml"
search = "version = \"{current_version}\""
replace = "version = \"{new_version}\""

[[tool.bumpversion.files]]
filename = "./uv.lock"
search = """name = "openutility"
version = "{current_version}"
source = """
replace = """name = "openutility"
version = "{new_version}"
source = """
