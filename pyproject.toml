[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "OpenUtility"
version = "0.1.0"
description = "Pyomo-based utility-system optimization model and reporting helpers."
readme = "README.md"
requires-python = ">=3.14.2"
license = "MIT"
authors = [
  { name = "Tim Walmsley", email = "tim.walmsley@waikato.ac.nz" },
]
dependencies = [
  "CoolProp>=6.6",
  "highspy>=1.15.1",
  "matplotlib>=3.8",
  "numpy>=1.26",
  "OpenPinch>=0.4.5",
  "pandas>=2.0",
  "pyomo>=6.10.0",
]

[project.scripts]
openutility-style-table2-9 = "case_study.jimenez_romero_utility_system_optimization.cli:main"

[project.optional-dependencies]
notebook = [
  "jupyterlab>=4",
  "nbformat>=5",
]
docs = [
  "sphinx>=7",
]
dev = [
  "pytest>=8.0",
  "ruff>=0.8",
]

[tool.hatch.build.targets.wheel]
packages = ["OpenUtility", "case_study"]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff]
line-length = 88
